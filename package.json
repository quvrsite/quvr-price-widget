{
  "name": "@quiver/widget",
  "version": "0.2.0",
  "description": "Embeddable price ticker & mini-watchlist widget for Quiver (Stock Tokens / USDG on Robinhood Chain). Drop it on any site with one script tag. Safe by default — shares @quiver/sdk's caching/backoff so it can't hammer the backend.",
  "main": "dist/quiver-widget.js",
  "module": "dist/quiver-widget.mjs",
  "files": ["dist"],
  "scripts": {
    "build": "tsup src/index.ts --format iife,esm --global-name QuiverWidget --minify",
    "dev": "tsup src/index.ts --format iife --global-name QuiverWidget --watch"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/quvrsite/quiver-widget.git"
  },
  "dependencies": {
    "@quiver/sdk": "^0.2.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.5.0"
  }
}
